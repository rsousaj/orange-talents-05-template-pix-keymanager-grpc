micronaut:
  application:
    name: keymanagerGrpc
datasources:
  default:
    url: jdbc:h2:mem:keymanager;MODE=MYSQL
    driverClassName: org.h2.Driver
jpa.default.properties.hibernate.hbm2ddl.auto: update